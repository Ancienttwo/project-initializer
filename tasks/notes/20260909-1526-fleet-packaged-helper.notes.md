# Dispatch boundary

Reuse the existing trusted helper runner as the sole runtime authority. Its current cli/runtime location is retained; moving the utility is not needed to fix acquisition. Plan projection runs in the already-linked worktree, where its existing topology check prevents nested provision. The canary failure was reconciled from its observed failed provision and released lease, not treated as an unknown successful acquisition.
