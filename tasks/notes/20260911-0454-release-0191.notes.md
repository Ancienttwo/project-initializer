# Release Preparation Notes

The release candidate is pinned to final PR #408 source 03516106dc843b694241023097a85ab89ee30807, including merged #401. The full release gate can run while GitHub CI completes because the product source is frozen. Before publication, verify the #408 main merge retains that same product content. No registry write, tag, public release or global installation is performed by this slice.

Version authority checks passed for package/template and all five README stamps. Archctx dependencies remain exactly 0.5.10.

> **Substantive Change SHA256**: `sha256:a00c1bb3b9eb1ff78f50b1370156baedbe6b8d9c9a882fe7fc574226e90c881f`

## Release hold

Both functionality PRs are merged. Final main tree equals the pinned feature candidate exactly; merging it into this branch had zero file delta. The full gate on 7fc8f5bd timed out at 3,598,202 ms and recorded two descendant.pid fixture failures. Diagnostics: `.ai/harness/runs/verification-vx-597e891cefc94c99a3a7.log`; event evt-01M26NDGV8XAJTWAXSBEFVBF0Y. Preserve this evidence; no second full run and no unrelated process-supervision fix were attempted. The release filing and this note now report the hold; no release acceptance is claimed.

> **Substantive Change SHA256**: `sha256:5049ecae3e938418825f2c68446b86ceb773c2ae9f717dfdb7fd1beda5485d80`

## Fixture Repair Outcome

The two failures were reproduced together before editing (`/tmp/0191-process-tree-pre-fix.log`, PRE_FIX_EXIT=1). Their original 100/500 ms deadlines could expire during runtime selection or process startup, before descendant.pid was written. The parent also published PID before the descendant proved it had installed its TERM handler.

Both consumers now use one test-only fixture. The descendant installs its handler and publishes its own ready PID, then records any received SIGTERM. The tests require a real timeout and disappearance of the child (plus the full process group in the bundled test); POSIX cases also require the TERM receipt, proving the escalation path is exercised. Startup is deliberately non-instant. Hook supervision has a 5-second integration budget; the provider test additionally budgets for Node selection, for 10 seconds total. Production deadlines and cleanup code are unchanged. Windows retains child-death verification without claiming POSIX signal delivery.

Validation: both complete files passed, **35 tests / 193 assertions**, in 27.04 seconds (`/tmp/0191-process-tree-full-files.log`). Typecheck, hook/helper/reference parity, deploy SQL order, architecture sync, state inspection and init dry-run passed. The full release gate was not rerun; its earlier whole-run timeout and unexecuted packed installation smoke remain a release hold.

> **Substantive Change SHA256**: `sha256:1e716f4d1cb0874b79c331b2b550f1c7c6292e16151e9b5d6f3ab8dec24e8fa6`
